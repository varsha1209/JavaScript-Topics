console.log(name);  //varsha
// var name;  // output is empty string because name variable alredy present in the global scope. it has empty string by default.
var name = "varsha"; // afte assigning the varsha it will always print varsha untill we will open a new tap
